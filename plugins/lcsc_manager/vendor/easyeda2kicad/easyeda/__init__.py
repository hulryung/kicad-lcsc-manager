"""Vendored easyeda submodule — footprint import path only.

Upstream re-exports symbol and SVG-renderer classes here too; we don't
ship those, so keep this empty. Import the submodules directly.
"""
